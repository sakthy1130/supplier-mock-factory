{
  "_readme": [
    "Config for the contract_br provisioning depth: which BR rule-value-mapping bodies to",
    "create so a CONTRACT (rather than an apiKey) drives the markup rules.",
    "",
    "Each item under 'conditions' is POSTed to /rulevaluemappings verbatim, except",
    "inputValue, which SMF fills in from the contracts it just created.",
    "",
    "  contract_value_field : which contract attribute becomes inputValue. 'autoId' is the",
    "                         short numeric id the real 'contractId IN' condition matches",
    "                         (e.g. 10103) — SMF reads it back from Backoffice after",
    "                         creating each contract. 'uid' and 'id' (mongo _id) are also",
    "                         available.",
    "  value_mode           : 'join' (default) puts every contract in ONE condition as a",
    "                         separated list, which is what operator IN expects.",
    "                         'per_contract' creates one condition per contract instead,",
    "                         for a single-value operator such as EQUALS.",
    "  value_separator      : default ','.",
    "",
    "The stg body below came from a working request against STAGING. inputDetailId 30 is",
    "'contractId IN' (field contractId, operator IN, datatype STRING) and the condition",
    "hangs under the 'apikey IN' parent (id 5) of rule 3 (Static Markup).",
    "",
    "dev is {} because these ids are ENVIRONMENT data — the parent condition id in",
    "particular. Do not copy the stg block to dev without checking the ids there; a wrong",
    "parent id makes the BR step fail loudly rather than silently.",
    "",
    "Each rule's root condition ALREADY EXISTS in BR and is referenced by id, never",
    "created: rule 3 hangs under 'apikey IN' (id 5), rule 4 under 'marketPrice is NOT",
    "NULL' (id 6). Trying to create the rule-4 root returns",
    "400 AlreadyExistsException, which is how we learned its id. This mirrors the apiKey",
    "path, which references its parents (STATIC/DYNAMIC_MARKUP_PARENT_CONDITION_ID) too.",
    "",
    "Two extra knobs exist for a parent that genuinely does NOT pre-exist (unused here):",
    "",
    "  children             : nested conditions. Each is created with",
    "                         parentRuleValueMappingId set to the LIVE id of its parent,",
    "                         never a value hardcoded here.",
    "  inject_input_value   : false means 'do not put the contract list in this body' —",
    "                         for an operator-only test where a value would change what",
    "                         it matches.",
    "",
    "Teardown deletes what SMF created, in reverse creation order (BR refuses to delete a",
    "parent while a child points at it). A condition that came back as already-existing is",
    "never deleted — it was not ours to create.",
    "",
    "To disable for an env, set it to {} — contract_br then still creates the mocks and",
    "contract and reports the BR step as NOT_CONFIGURED."
  ],
  "dev": {},
  "stg": {
    "contract_value_field": "autoId",
    "value_mode": "join",
    "conditions": [
      {
        "ruleId": "3",
        "description": "Contract ID IN the List",
        "executionOrder": 4,
        "inputDetailId": 30,
        "outputDetail": null,
        "outputValue": "",
        "outputType": "",
        "overwrite": false,
        "parentRuleValueMapping": { "id": 5, "description": "apikey IN" },
        "parentRuleValueMappingId": null
      },
      {
        "ruleId": "4",
        "description": "Contract ID IN the List",
        "executionOrder": 4,
        "inputDetailId": 30,
        "outputDetail": null,
        "outputValue": "",
        "outputType": "",
        "overwrite": false,
        "parentRuleValueMapping": { "id": 6, "description": "marketPrice is NOT NULL" },
        "parentRuleValueMappingId": null
      }
    ]
  }
}
