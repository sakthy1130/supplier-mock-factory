USe : staging Hotels BR: likely http://hotel-connectivity-br.tajawal-stg.internal
After creating the APIKEY I want you to creat BusinessRule for the created APIKEY
-First Add and Assign creaetd apiKEy to the RuleId 3(Room Static Markup In Percentage) 
-Then Assign creaetd apiKEy to the RuleId 4(Room Dynamic Markup In percentage) 
-Inside ruleId 3 want to creat condition with parent id as 178 with name APIKey Included and add the creted apiKEy
-Inside ruleId 4 want to creat condition with parent id as 176 with name APIKey Included and add the creted apiKEy
-Clear the cachey
-Stricy tell your understning for this and how you will implement this
-No code
-Ask as many as question u need



So now I want you to create one more screen for crawler mock.
So in the existing crawler Mock screen, we have 5 buckets:

- crawler lower
- Expedia lower
- Equal
- only Expedia
- only crawler
Now I want you to develop a screen where all these five flows should be called separately, one after the other by clicking a button.
Flow is creating the mock for each and every bucket:

- crawler lower
- expedia lower
- equal
- only expedia
- only crawler
For each and every flow, the run scenario should be called, and the output should be populated on the screen.
After that the clear data should be called, then, all the created data should be deleted with the existing process for clear data. Once clear is done, then only the next flow should be called.
Strictly once the run scenario is completed, then only the next flow should be called.
This process should be in a queued UI, so after completing each and every flow, the UI should be updated with the corresponding output data.
Strictly make sure the existing flow is not getting impacted because of the new development.

